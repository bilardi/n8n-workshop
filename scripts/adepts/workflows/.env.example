# Configuration for import_workflows.py
# Copy to .env and fill with real values (.env is gitignored)

# Base URL of the n8n instance (no trailing slash)
N8N_BASE_URL=http://localhost:5678

# Member credentials: workflows are created in this user's personal project
N8N_EMAIL=member@example.com
N8N_PASSWORD=change-me
